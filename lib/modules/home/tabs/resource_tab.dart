import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ResourceTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('no_store_available'.tr),
    );
  }
}
